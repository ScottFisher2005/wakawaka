// 🎨 Magic Colors and Brushes (PostCSS Configuration)
// We use these magic tools to make our A, B, C letters colorful and bright!

const magicColorConfig = {
  plugins: {
    // 🖌️ Our special Tailwind brushes for painting the alphabet
    "@tailwindcss/postcss": {},
  },
};

// 🎈 Give the colors to the classroom!
export default magicColorConfig;
